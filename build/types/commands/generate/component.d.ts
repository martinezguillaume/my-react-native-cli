import { GluegunToolbox } from 'gluegun';
export declare const description = "Generates a component, supporting files, and a storybook test.";
export declare const run: (toolbox: GluegunToolbox) => Promise<void>;
